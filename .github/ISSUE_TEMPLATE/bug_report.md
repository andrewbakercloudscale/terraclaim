---
name: Bug report
about: Something is not working correctly
---

**Script and version**
Which script: `terraclaim.sh` / `drift.sh` / `run.sh` / `reconcile.sh`
Version (`./terraclaim.sh --version`):

**Command run**
Paste the exact command (redact account IDs if needed):

**Debug output**
Run with `--debug` and paste the output:

**Environment**
- AWS CLI version (`aws --version`):
- Terraform version (`terraform version`):
- OS:
- Bash version (`bash --version`):

**Expected behaviour**

**Actual behaviour**
